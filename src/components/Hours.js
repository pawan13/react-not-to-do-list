import React from 'react'

const Hours = ({ttl}) => {
  return (
    <div>
       <p>You could have saved = {ttl} hr</p>
          {/* total hours  */}
          <div>
            Total hour per week allocated = {}hr
          </div>
    </div>
  )
}

export default Hours
